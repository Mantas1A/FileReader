# Allowed MIME types (comma-separated)
ALLOWED_MIME_TYPES=text/csv,application/csv,text/xml,application/xml,application/json
# Allowed extension types (comma-separated)
ALLOWED_FILE_EXTENSION_TYPES=csv,xml,json
